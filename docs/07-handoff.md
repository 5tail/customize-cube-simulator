# 07 交接檔（每個 session 結束前必須更新）

> 這是 session 之間唯一的記憶。格式固定如下，最新的寫最上面，只留最近 3 個 session（更舊的移到 docs/handoff-archive.md）。

---

## Session 0 — 2026-07-03（制度建立，在 claude.ai 對話介面由 Fable 5 產生）

**做了什麼**：建立整套制度檔案（docs/00–09 ＋ CLAUDE.md ＋ README）。專案程式碼**尚未開始**，repo 還不存在。

**目前狀態**：
- 架構已定案（見 docs/02），規格已定案（見 docs/01）。
- 下一步 = README「第一次使用」章節的步驟 1–4（建 repo、放入本檔案包），然後才是 Phase 1 第一個任務。

**下一步（給下個 session）**：
1. 確認使用者已把檔案包放上 GitHub repo。
2. 用 docs/05 的 T2 模板開始 Phase 1 第一個子任務：「建立 Vite + React 專案骨架＋一顆能拖曳旋轉的素色 3x3 方塊，部署到 GitHub Pages」。這個子任務刻意小，目的是把「push → CI → 上線」的水管先打通。

**未解問題 / 待使用者決定**：
- Supabase 與 Resend 帳號尚未建立（Phase 2 才需要，到時模型要提供逐步開通指引，帳號註冊由使用者本人操作）。
- 網站名稱、網域未定（GitHub Pages 預設網址即可先用）。

**本次教訓**：無（首個 session）。

---

（下個 session 從這裡往上加新區塊）
